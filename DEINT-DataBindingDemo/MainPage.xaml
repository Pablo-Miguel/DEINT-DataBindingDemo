<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:Model="clr-namespace:DEINT_DataBindingDemo.Model"
             x:Class="DEINT_DataBindingDemo.MainPage">
    
    <!--Enlace del label con una propiedad-->
    <!--<ContentPage.Resources>
        <Model:Person x:Key="person" Nombre="Juan" Direccion="Larga" Edad="23" />
    </ContentPage.Resources>

    <Label Text="{Binding Nombre, Source={StaticResource person}}" />-->

    <!--Enlace del label con la clase-->
    <!--<StackLayout VerticalOptions="Center" >
        
        <Label x:Name="NombreTxt" HorizontalOptions="Center" />

        <Button Text="Click aqui" Clicked="Button_Clicked" />
        
    </StackLayout>-->

    <!--Enlace de la clase con todos los componentes-->
    <StackLayout VerticalOptions="Center" >

        <Label Text="{Binding Nombre}" HorizontalOptions="Center" />
        <Label Text="{Binding Direccion}" HorizontalOptions="Center" />
        <Label Text="{Binding Edad}" HorizontalOptions="Center" />

        <Button Text="Click aqui" Clicked="Button_Clicked" />

    </StackLayout>

</ContentPage>
